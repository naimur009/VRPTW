# ============================================================
# VRPTW Project Requirements
# Generated from actual imports across all Python source files
#
# Install with:
#   # CPU-only (default):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
#   # GPU (CUDA):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# NOTE: gurobipy requires a valid Gurobi license.
#       Free academic licenses available at https://www.gurobi.com/academia/
# ============================================================

# --- Optimization Solvers ---
gurobipy==13.0.1        # Gurobi optimizer (requires license)
pyvrp==0.13.3           # VRP solver (used in Genetic Algorithm module)
vrplib==2.2.0           # VRP benchmark instance reader (dependency of pyvrp)

# --- Deep Learning ---
# torch is NOT listed here — install it FIRST from a PyTorch index:
#   CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu124
# Then install the rest via this requirements file.
torch-geometric==2.8.0  # Graph Neural Network library (GINEConv, BatchNorm, DataLoader)
networkx==3.6.1         # Graph library (required by torch-geometric)

# --- Data Science & Numerics ---
numpy==2.4.6            # Array operations
pandas==3.0.3           # Data manipulation and CSV handling
scipy==1.17.1           # Statistical distributions (beta, gamma, GEV, Weibull)

# --- Terminal UI ---
rich==15.0.0            # Console output, progress bars, tables, panels